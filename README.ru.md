🌍 **[English](README.md)**

<div align="center">

# AI Response File Splitter

**Преобразует структурированные AI-ответы в полноценную структуру файлов проекта**

</div>

***

## 📋 Обзор

AI Response File Splitter — это Python-инструмент без внешних зависимостей, который автоматически превращает один структурированный ответ от AI в структуру проекта с нужными файлами и папками.

Вместо того чтобы вручную копировать куски кода из AI-чата в разные файлы, этот инструмент парсит специально отформатированный текстовый файл и создаёт для вас всю структуру проекта за считанные секунды.

### ✨ Ключевые возможности

- 🎯 **Читаемый человеком формат** — простой текстовый формат, не требуется JSON
- 🚀 **Без зависимостей** — используется только стандартная библиотека Python
- 🔒 **Валидация путей** — предотвращает переходы по директориям и опасные пути
- 👀 **Режим dry-run** — предварительный просмотр файлов без их создания
- 🛡️ **Безопасная перезапись** — интерактивные подтверждения при попытке перезаписи файлов
- 📊 **Детальное логирование** — режимы verbose, normal и quiet
- 📥 **Поддержка stdin** — работает с pipe и перенаправлениями
- ⚡ **Быстрота** — обрабатывает сотни файлов за секунды
- 🌍 **Кросс-платформенность** — работает на Windows, macOS и Linux

***

## 🔧 Требования

- Python 3.8 или выше (никаких дополнительных пакетов не требуется)

Все используемые модули входят в стандартную библиотеку Python:

- `os` — операции с файловой системой
- `sys` — системные функции
- `argparse` — разбор аргументов командной строки
- `logging` — логирование
- `typing` — аннотации типов

***

## 📥 Установка

### Быстрая установка

Скачайте скрипт:

```bash
curl -O https://raw.githubusercontent.com/avantyur1st/ai-file-splitter/main/split_ai_answer.py
chmod +x split_ai_answer.py
```

или клонируйте репозиторий:

```bash
git clone https://github.com/avantyur1st/ai-file-splitter.git
cd ai-file-splitter
```

Устанавливать через `pip` ничего не требуется!

***

## 🚀 Сценарии использования

Этот инструмент поддерживает **два варианта работы** в зависимости от момента структурирования кода:

### Сценарий 1: 📝 Структурируем код с самого начала (рекомендуется)

Используйте этот промпт **в начале** разговора с AI, чтобы сразу получать структурированный код.

### Сценарий 2: 🔄 Рефакторинг готового кода

Используйте этот промпт **в конце** разговора, чтобы преобразовать существующий код в многофайловый проект.

***

## 📝 Сценарий 1: Генерация структурированного кода с самого начала

### 1. Используйте этот промпт

Скопируйте и примените его **в начале** разговора с любым AI (ChatGPT, Claude и др.):

```
You are a multi-file project generator.

Based on the technical requirements below, generate code split into multiple files.

Format (strictly follow):
- For each file use this block:
  FILE relative/path/to/file.py
  ========================================
  <full file content>
  ========================================
  END FILE

- The line starting with FILE contains the path relative to project root
- Lines of ==== serve as separators (minimum 10 identical characters)
- Between two separators is ONLY the file content without extra comments or Markdown
- After the second separator must be the line END FILE

Below is the user's technical requirements:
<your project description here>
```


### 2. Сохраните ответ AI

Сохраните ответ AI в текстовый файл, пример: `ai_response.txt`

### 3. Запустите скрипт

```bash
python split_ai_answer.py ai_response.txt -o ./my_project
```

Готово! Структура проекта появится в папке `./my_project/`

***

## 🔄 Сценарий 2: Рефакторинг готового кода

Если код уже готов и вы хотите преобразовать его в полноценный проект, используйте этот промпт **в конце** разговора:

### Промпт для рефакторинга

```
Please take all the code we've created in this chat and transform it into a multi-file project structure.

Output format (strictly follow):
- For each file use this block:
  FILE relative/path/to/file.py
  ========================================
  <full file content>
  ========================================
  END FILE

Requirements:
1. Split code into logical modules and files
2. Create proper directory structure (e.g., src/, tests/, config/, docs/)
3. Add requirements.txt (if dependencies are needed)
4. Add .gitignore for the project
5. Create README.md with brief project description
6. Each file should be self-contained (with all necessary imports)
7. Follow best practices for project structure in Python/JavaScript/other language

Do not add any explanations before or after file blocks — only the formatted code itself.
```


### Альтернативный короткий вариант

```
Reformat all code from this chat into a multi-file project.

Use format:
FILE path/to/file.ext
========================================
<content>
========================================
END FILE

Split into logical modules, add folder structure, README.md, .gitignore, and requirements.txt (if needed).
```


### Запуск скрипта

```bash
python split_ai_answer.py ai_response.txt -o ./my_project
```


***

## 📖 Справка по командной строке

### Основной синтаксис

```bash
python split_ai_answer.py [INPUT] [OPTIONS]
```


### Опции

| Опция | Описание |
| :-- | :-- |
| `INPUT` | Путь к файлу с ответом AI (или `-` для stdin) |
| `-o, --output-dir DIR` | Каталог для создаваемых файлов (по умолчанию текущий) |
| `--encoding ENCODING` | Кодировка выходных файлов (по умолчанию utf-8) |
| `--dry-run` | Предварительный просмотр файлов без создания |
| `--force` | Перезаписывать существующие файлы без вопроса |
| `-v, --verbose` | Подробный вывод (отладка) |
| `-q, --quiet` | Только ошибки (тихий режим) |
| `--version` | Показать версию |
| `-h, --help` | Показать помощь |


***

## 💡 Примеры применения

### Пример 1: Базовое использование

```bash
python split_ai_answer.py response.txt -o ./myapp
```

**Вывод:**

```
INFO: Found 5 file(s) to process
INFO: Created: ./myapp/main.py
INFO: Created: ./myapp/config.py
INFO: Created: ./myapp/utils/helpers.py
INFO: Created: ./myapp/models/user.py
INFO: Created: ./myapp/tests/test_main.py

Summary: Created 5 file(s), skipped 0, errors 0
```


### Пример 2: Просмотр без создания (dry-run)

```bash
python split_ai_answer.py response.txt -o ./myapp --dry-run
```

**Вывод:**

```
INFO: Would create: ./myapp/main.py (1247 bytes)
INFO: Would create: ./myapp/config.py (523 bytes)
INFO: Would create: ./myapp/utils/helpers.py (892 bytes)

Summary: Would create 3 file(s), skipped 0, errors 0
```


### Пример 3: Из stdin (поддержка pipe)

```bash
cat ai_response.txt | python split_ai_answer.py - -o ./project
```

Или из буфера обмена:

```bash
pbpaste | python split_ai_answer.py - -o ./project  # macOS
xclip -o | python split_ai_answer.py - -o ./project  # Linux
```


### Пример 4: Принудительная перезапись файлов

```bash
python split_ai_answer.py response.txt -o ./myapp --force
```


### Пример 5: Режим отладки (verbose)

```bash
python split_ai_answer.py response.txt -o ./myapp -v
```


### Пример 6: Тихий режим (только ошибки)

```bash
python split_ai_answer.py response.txt -o ./myapp -q
```


***

## 📝 Формат входных данных

Ответ AI должен содержать блоки файлов в строгом формате:

```text
FILE path/to/file.py
========================================
<full file content>
========================================
END FILE

FILE another/file.js
========================================
<full file content>
========================================
END FILE
```


### Правила формата

- **Строка заголовка:** `FILE <relative/path/to/file>`
- **Разделитель:** строка из ≥10 одинаковых символов `=` или `-`
- **Содержимое:** всё между двумя одинаковыми разделителями
- **Строка окончания:** `END FILE` на отдельной строке
- **Пути:** релативные (без `..` и абсолютных)


### Корректный пример

```text
FILE src/main.py
========================================
#!/usr/bin/env python3

def main():
    print("Hello, World!")

if __name__ == "__main__":
    main()
========================================
END FILE

FILE src/utils/config.py
========================================
CONFIG = {
    "version": "1.0.0",
    "debug": True
}
========================================
END FILE
```


***

## 🔒 Безопасность

Скрипт включает проверки безопасности:

- ✅ предотвращение traversal — блокирует пути с `..`
- ✅ запрещает абсолютные пути (начинающиеся с `/` или `\`)
- ✅ защита от выхода за пределы директории
- ✅ подтверждение при перезаписи (если не указан `--force`)

***

## 🐛 Решение проблем

### "Separator not found after FILE"

Причина: отсутствует или неверен разделитель после строки `FILE`

Решение: после строки `FILE` должна идти строка с не менее 10 `=` или `-`

### "Expected 'END FILE' after file"

Причина: отсутствует или неверная строка окончания блока файла

Решение: каждый файл должен заканчиваться строкой `END FILE` на отдельной строке

### "Path escapes output directory"

Причина: путь пытается выйти за пределы директории вывода

Решение: используйте только относительные пути без `..` и ведущих `/`

### "No file blocks found in input"

Причина: файл не содержит правильных блоков файлов

Решение: убедитесь, что ответ AI строго соответствует формату

### Проблемы с отступами после копирования

**Причина:** некоторые чаты (например, Perplexity) портят отступы при копировании кода.

**Решение:** попросите модель выводить каждый блок кода внутри текстового блока `text""" <код здесь> """`, тогда пробелы и табы сохранятся при вставке.
***

## 🤝 Контрибьютинг

Приветствуются любые вклады:

1. Сделайте fork репозитория
2. Создайте ветку (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

Рекомендации:

- придерживайтесь PEP8
- добавляйте аннотации типов
- обновляйте тесты и документацию

***

## 📄 Лицензия

MIT

***

## 👤 Автор

[@avantyur1st](https://github.com/avantyur1st)

***

## 🙏 Благодарности

- Вдохновлено необходимостью быстро создавать проекты из AI-кода
- Спасибо AI-моделям, которые делают код лучше
- Реализовано на стандартной библиотеке Python

***

## 📊 Статистика

- Строк кода: ~250
- Зависимостей: 0
- Версия Python: 3.8+
- Лицензия: MIT
- Готово к использованию

***

<div align="center">

⭐ Если вам помог проект — поставьте звезду!
